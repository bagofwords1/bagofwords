apiVersion: v1
kind: Service
metadata:
  name: bowapp
  namespace: {{.Release.Namespace}}
spec:
  type: ClusterIP
  selector:
    app: bowapp
  ports:
    - name: http
      port: 8000
      targetPort: 8000