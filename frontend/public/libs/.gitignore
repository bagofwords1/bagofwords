# Vendored JS libraries are downloaded during Docker build
# via scripts/download-vendor-libs.sh
# Do not commit the actual JS files to the repository.
*.js
!artifact-globals.js
!artifact-offline-host.js
!artifact-tailwind.js
# Vendored fonts (+ the generated @font-face sheet) are downloaded too.
fonts/
artifact-fonts.css
